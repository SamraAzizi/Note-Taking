# Update the file with correct publish directory
@"
[build]
  command = \"npm run build\"
  publish = \"dist\"

[[redirects]]
  from = \"/*\"
  to = \"/index.html\"
  status = 200
"@ | Out-File -FilePath "netlify.toml" -Encoding ASCII